# Model artifacts

Drop your trained model files here after running the Colab notebook:

- `lstm_wco.pt` — the LSTM state_dict exported by `notebooks/lstm_training.ipynb`
- `scaler.npz` — min/max scaling values saved during training

These files are gitignored (they can be large). Re-export them whenever you retrain.
